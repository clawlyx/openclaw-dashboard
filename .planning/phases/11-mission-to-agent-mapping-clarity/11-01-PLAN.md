---
phase: 11-mission-to-agent-mapping-clarity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/agents.ts
  - lib/openclaw.ts
  - demo/openclaw-home/agents/dashboard.json
autonomous: true
requirements:
  - MAP-01
  - MAP-02
user_setup: []
must_haves:
  truths:
    - Working agents expose explicit Mission Control mapping metadata that distinguishes exact, partial, and unavailable task context instead of forcing the client to infer it from room or workload labels.
    - Any linked task or feature context remains advisory about the current agent view and keeps Mission Control as the ownership source of truth.
    - Demo data covers exact, partial, and unavailable mapping cases so Phase 11 can be verified without relying on one local runtime dataset.
  artifacts:
    - lib/agents.ts
    - lib/openclaw.ts
    - demo/openclaw-home/agents/dashboard.json
  key_links:
    - Phase 10 already added workload provenance and advisory next-move metadata, so Phase 11 should extend that server contract with Mission Control joins instead of rebuilding task inference in the UI.
---

<objective>
Build the Mission Control mapping contract for Phase 11.

Purpose: give the Agents surfaces one trustworthy server-side description of whether a working agent maps to a live Mission Control task, what evidence supports that mapping, and how confidently the operator should read it.
Output: additive agent snapshot metadata and demo fixtures that expose exact, partial, and unavailable Mission Control mapping states.
</objective>

<execution_context>
@/Users/clawlyx/.codex/get-shit-done/workflows/execute-plan.md
@/Users/clawlyx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-agent-work-provenance-and-concise-coordination-view/10-RESEARCH.md
@.planning/phases/10-agent-work-provenance-and-concise-coordination-view/10-VERIFICATION.md
@lib/agents.ts
@lib/openclaw.ts
@lib/mission-control.ts
@demo/openclaw-home/agents/dashboard.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the agent snapshot contract with Mission Control mapping metadata</name>
  <files>lib/agents.ts</files>
  <action>Add additive snapshot types for mission mapping state, linked task and feature identifiers, destination hints, and operator-facing source notes. Keep the contract backward-compatible so existing configured snapshots and derived fallbacks still render when no mapping metadata exists.</action>
  <verify>pnpm typecheck</verify>
  <done>The snapshot can describe exact, partial, and unavailable Mission Control mapping states for each agent without breaking older data sources.</done>
</task>

<task type="auto">
  <name>Task 2: Join workload provenance to live Mission Control context on the server</name>
  <files>lib/openclaw.ts, lib/agents.ts</files>
  <action>Build server-side join logic that resolves current agent evidence into a Mission Control mapping object when a task or feature can be matched, marks partial cases when only nearby context exists, and leaves unavailable cases explicit when no trustworthy link exists. Preserve the product boundary: Mission Control owns task truth, while Agents only summarizes the most relevant linked context.</action>
  <verify>pnpm typecheck</verify>
  <done>Agents snapshot consumers receive one normalized Mission Control mapping object instead of needing to guess from `currentTaskId`, workload labels, or room ownership alone.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh demo mapping fixtures for deterministic exact, partial, and unavailable cases</name>
  <files>demo/openclaw-home/agents/dashboard.json</files>
  <action>Update the bundled demo snapshot so at least one working agent has an exact Mission Control task link, one has only partial nearby Mission Control context, and one clearly reports that no live mapping is available. Ensure the fixture still demonstrates repo-work provenance and personal-research provenance from Phase 10.</action>
  <verify>pnpm build</verify>
  <done>The repo ships with deterministic data that proves every mapping state Phase 11 needs before UI work begins.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `pnpm build`
- [ ] `/api/snapshot` or demo snapshot inspection confirms exact, partial, and unavailable Mission Control mapping states are present in the server contract
- [ ] Code inspection confirms Mission Control remains the ownership system of record and Agents only references linked context
- [ ] Demo data review confirms at least one exact task link, one partial contextual link, and one unavailable mapping case
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The Phase 11 UI can rely on one explicit Mission Control mapping contract instead of inferring linkage from ad hoc fields
</success_criteria>

<output>
After completion, create `.planning/phases/11-mission-to-agent-mapping-clarity/11-01-SUMMARY.md`
</output>
