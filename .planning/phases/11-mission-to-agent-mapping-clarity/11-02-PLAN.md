---
phase: 11-mission-to-agent-mapping-clarity
plan: 02
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - components/agents-virtual-office-panel.tsx
  - components/agents-office-panel.tsx
  - app/globals.css
  - locales/en.json
  - locales/zh.json
autonomous: true
requirements:
  - MAP-01
  - MAP-02
  - MAP-03
user_setup: []
must_haves:
  truths:
    - The default Agents scan path shows the linked Mission Control task context inline when a trustworthy mapping exists and makes partial or unavailable states obvious when it does not.
    - Mapping labels and supporting copy never imply that ownership moved into Agents; they explicitly point back to Mission Control as the system of record.
    - Operators can start a handoff from Agents into Mission Control without manually searching, and unavailable mappings do not expose misleading navigation affordances.
  artifacts:
    - components/agents-virtual-office-panel.tsx
    - components/agents-office-panel.tsx
    - app/globals.css
    - locales/en.json
    - locales/zh.json
  key_links:
    - Phase 10 already established the concise coordination scan path in the virtual office, so Phase 11 should enhance that path with mission mapping clarity rather than reintroducing dense secondary rails.
---

<objective>
Expose Phase 11 mapping clarity directly in the Agents surfaces.

Purpose: turn the new server-side mapping contract into a trustworthy operator experience that shows the linked Mission Control task when available, labels confidence clearly, and provides one obvious path into Mission Control from the Agents scan flow.
Output: updated Agents views, localized mapping copy, and responsive styling for exact, partial, and unavailable mapping states.
</objective>

<execution_context>
@/Users/clawlyx/.codex/get-shit-done/workflows/execute-plan.md
@/Users/clawlyx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-mission-to-agent-mapping-clarity/11-01-PLAN.md
@components/agents-virtual-office-panel.tsx
@components/agents-office-panel.tsx
@app/globals.css
@locales/en.json
@locales/zh.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Show linked Mission Control task context in the working Agents scan path</name>
  <files>components/agents-virtual-office-panel.tsx, components/agents-office-panel.tsx, locales/en.json, locales/zh.json</files>
  <action>Update the working-agent cards, detail drawers, and supporting lists so they show the linked Mission Control task or feature context when mapping is exact or partial. Make the unavailable state explicit and readable instead of leaving the operator to infer whether a link exists.</action>
  <verify>pnpm lint</verify>
  <done>Operators can see the current linked Mission Control context from Agents when it exists, and they can tell when the UI only has partial or no live mapping.</done>
</task>

<task type="auto">
  <name>Task 2: Add clear Mission Control handoff affordances from Agents</name>
  <files>components/agents-virtual-office-panel.tsx, components/agents-office-panel.tsx, locales/en.json, locales/zh.json</files>
  <action>Introduce one obvious Mission Control action in the relevant Agents cards and detail surfaces that navigates to the best available Mission Control destination for the mapping state. Exact mappings should target the linked task context, partial mappings should land on the closest trustworthy feature or queue context, and unavailable mappings should stay visibly non-actionable.</action>
  <verify>pnpm typecheck</verify>
  <done>The operator no longer has to manually search Mission Control after identifying the relevant agent on the Agents surface.</done>
</task>

<task type="auto">
  <name>Task 3: Style and localize mapping confidence without ownership ambiguity</name>
  <files>components/agents-virtual-office-panel.tsx, components/agents-office-panel.tsx, app/globals.css, locales/en.json, locales/zh.json</files>
  <action>Refine labels, badges, helper copy, and responsive layout so exact, partial, and unavailable mapping states remain scannable on desktop and smaller screens. The language should reinforce that Mission Control still owns task truth and that Agents is presenting linked context, not reassigning ownership.</action>
  <verify>pnpm build</verify>
  <done>Mapping confidence and ownership boundaries are visually and verbally clear across the shipped Agents views.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `pnpm build`
- [ ] Browser verification on `/?view=agents&panel=virtual` confirms working agents show exact, partial, or unavailable mapping state inline without ownership confusion
- [ ] Browser verification confirms at least one Agent entry exposes a Mission Control navigation action and that unavailable mappings do not present a false link
- [ ] Mobile and narrow-width review confirms the mapping state badges, helper copy, and action affordances remain readable in the default scan path
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The default Agents scan path makes linked Mission Control context visible and actionable without implying ownership moved out of Mission Control
</success_criteria>

<output>
After completion, create `.planning/phases/11-mission-to-agent-mapping-clarity/11-02-SUMMARY.md`
</output>
