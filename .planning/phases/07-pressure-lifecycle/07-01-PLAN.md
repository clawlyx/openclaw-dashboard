---
phase: 07-pressure-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/pressure-signals.ts
  - lib/mission-control.ts
  - lib/openclaw.ts
  - components/agents-virtual-office-panel.tsx
  - app/globals.css
  - locales/en.json
  - locales/zh.json
autonomous: true
requirements:
  - HIST-03
  - ROOM-03
  - OPER-03
user_setup: []
must_haves:
  truths:
    - Operators can tell whether pressure is new, sustained, worsening, or recovering without losing sight of the current snapshot state.
    - Room and mission detail surfaces explain why a signal is slipping or recovering with visible evidence instead of opaque scoring.
    - Lifecycle cues stay readable in demo and fallback states even when some history fields are partial or missing, including `/api/snapshot` responses.
  artifacts:
    - lib/pressure-signals.ts
    - lib/mission-control.ts
    - lib/openclaw.ts
    - components/agents-virtual-office-panel.tsx
    - locales/en.json
    - locales/zh.json
  key_links:
    - Phase 7 must interpret the historical metrics from Phases 5 and 6 rather than replace the existing pressure model with a new scoring system.
---

<objective>
Add pressure lifecycle states and improving-vs-worsening cues.

Purpose: let the operator see whether pressure is new, ongoing, slipping, or recovering directly in the workstation's summary-first triage flow.
Output: a lifecycle interpretation layer, concise reason copy in summary surfaces, and richer supporting evidence in detail views.
</objective>

<execution_context>
@/Users/clawlyx/.codex/get-shit-done/workflows/execute-plan.md
@/Users/clawlyx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-pressure-lifecycle/07-CONTEXT.md
@.planning/phases/07-pressure-lifecycle/07-VALIDATION.md
@lib/openclaw.ts
@.planning/phases/06-intelligence-surfaces/06-01-SUMMARY.md
@.planning/phases/06-intelligence-surfaces/06-02-SUMMARY.md
@lib/mission-control.ts
@lib/pressure-signals.ts
@components/agents-virtual-office-panel.tsx
@app/globals.css
@locales/en.json
@locales/zh.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive lifecycle states from visible status changes and trend-safe history</name>
  <files>lib/pressure-signals.ts, lib/mission-control.ts, lib/openclaw.ts</files>
  <action>Add a lightweight lifecycle interpretation layer that prioritizes operator-visible status changes, then uses duration and recent trend direction as secondary evidence. Keep the result explainable, fallback-safe, and compatible with partial history sources instead of introducing a separate opaque score, and thread the lifecycle fields into the dashboard snapshot contract so `/api/snapshot` exposes the same explainable lifecycle payload used by the UI.</action>
  <verify>pnpm typecheck</verify>
  <done>The data model and dashboard snapshot expose lifecycle state and supporting reason fields for tasks and rooms without breaking existing snapshot behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Surface lifecycle cues in operator summary and pressure rail</name>
  <files>components/agents-virtual-office-panel.tsx, app/globals.css</files>
  <action>Thread lifecycle labels and short reason lines into the operator summary and pressure rail so the operator can distinguish new pressure from long-running or recovering pressure at a glance. Keep lifecycle visibility summary-first and avoid adding a badge to every card by default.</action>
  <verify>pnpm lint</verify>
  <done>The workstation's primary triage surfaces communicate whether pressure is slipping, sustained, or recovering before the operator opens detail views.</done>
</task>

<task type="auto">
  <name>Task 3: Expand detail-view evidence and localized lifecycle copy</name>
  <files>components/agents-virtual-office-panel.tsx, locales/en.json, locales/zh.json</files>
  <action>Use progressive disclosure in room and mission detail views to explain the lifecycle judgment with recent metrics, history-source context, and localized copy in English and Chinese. Make the explanations trustworthy even when history data is partial.</action>
  <verify>pnpm build</verify>
  <done>Detail views explain why a signal is worsening or recovering and stay readable in both full-history and fallback modes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `pnpm build`
- [ ] Browser-verified operator summary distinguishes new, sustained, worsening, and recovering pressure in demo mode
- [ ] Browser-verified room detail and mission detail explain lifecycle reasoning with recent metrics or safe fallback copy
- [ ] `/api/snapshot` verification confirms the shared dashboard snapshot includes lifecycle fields and reason text for full-history, partial-history, and current-only records
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The workstation answers "is this getting worse or better?" without hiding the current-state truth
</success_criteria>

<output>
After completion, create `.planning/phases/07-pressure-lifecycle/07-01-SUMMARY.md`
</output>
