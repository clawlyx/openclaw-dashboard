---
phase: 02-detail-drawers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/agents-virtual-office-panel.tsx
  - app/globals.css
  - locales/en.json
  - locales/zh.json
autonomous: true
requirements:
  - DRAW-01
  - DRAW-02
user_setup: []
must_haves:
  truths:
    - The office surface exposes one shared detail-drawer state that can represent room focus and agent focus without splitting navigation into separate modes.
    - Room sprites, room cards, desk cards, and mission queue cards all route into the same focused detail surface.
    - The office scene remains readable while the detail drawer is open on desktop and mobile layouts.
  artifacts:
    - components/agents-virtual-office-panel.tsx
    - app/globals.css
    - locales/en.json
    - locales/zh.json
  key_links:
    - The drawer shell builds on the existing room-focus and mission-ownership joins already computed in `components/agents-virtual-office-panel.tsx`.
---

<objective>
Create the shared drawer shell and navigation model for the office view.

Purpose: Give the office a single focused detail surface that room and agent entry points can open consistently.
Output: Shared detail-selection state, room and agent entry wiring, responsive drawer shell, and localized copy for the new navigation and empty-state affordances.
</objective>

<execution_context>
@/Users/clawlyx/.codex/get-shit-done/workflows/execute-plan.md
@/Users/clawlyx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-detail-drawers/02-CONTEXT.md
@components/agents-virtual-office-panel.tsx
@app/globals.css
@locales/en.json
@locales/zh.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a shared detail-focus state and selectors</name>
  <files>components/agents-virtual-office-panel.tsx</files>
  <action>Replace the current room-only focus assumption with a shared detail selection model that can represent room focus and agent focus while still preserving the existing “all rooms” view. Reuse current room-focus helpers where possible and keep the state legible enough for follow-on content work.</action>
  <verify>pnpm typecheck</verify>
  <done>The panel can track a focused room or a focused agent through one consistent state model without breaking current room filtering behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Wire every office entry point into the shared drawer</name>
  <files>components/agents-virtual-office-panel.tsx</files>
  <action>Make room sprites, room summary cards, mission queue cards, and desk cards open the same detail surface. Mission queue cards should route to the exact owner context when an explicit owner agent is available and otherwise fall back to the owning room with an inferred label.</action>
  <verify>pnpm typecheck</verify>
  <done>Every supported entry point opens the same drawer shell and the selected context remains coherent when switching between room and agent triggers.</done>
</task>

<task type="auto">
  <name>Task 3: Build the responsive drawer shell and localization</name>
  <files>components/agents-virtual-office-panel.tsx, app/globals.css, locales/en.json, locales/zh.json</files>
  <action>Add the read-only drawer shell beside or beneath the office rails, depending on viewport size. Localize the shell headings, context badges, close actions, and placeholder copy. Preserve the office scene as the primary visual anchor instead of replacing it with a modal takeover.</action>
  <verify>pnpm build</verify>
  <done>The office renders a stable drawer container on desktop and mobile, with localized framing copy and a clear way to enter or clear focus.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `pnpm build`
- [ ] Room, desk, and mission entry points all open the same detail drawer shell
- [ ] The office remains usable on desktop and mobile while the drawer is open
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The drawer interaction model is unified instead of fragmented across room and agent entry points
</success_criteria>

<output>
After completion, create `.planning/phases/02-detail-drawers/02-01-SUMMARY.md`
</output>
