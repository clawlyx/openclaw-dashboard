---
phase: 02-detail-drawers
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - components/agents-virtual-office-panel.tsx
  - app/globals.css
  - locales/en.json
  - locales/zh.json
autonomous: true
requirements:
  - DRAW-01
  - DRAW-02
  - DRAW-03
user_setup: []
must_haves:
  truths:
    - Focused room and agent drawers show current task, last completed task, next planned step, and blocker or waiting context without hiding missing fields behind empty sections.
    - The drawer stays scoped to the selected owner context and surfaces recent handoff context from agent activity and mission task history.
    - Linked artifacts, branch details, and PR links appear when the associated feature exposes them, with safe placeholders when they do not.
  artifacts:
    - components/agents-virtual-office-panel.tsx
    - app/globals.css
    - locales/en.json
    - locales/zh.json
  key_links:
    - Drawer content joins selected mission tasks to `missionControl.features` so feature artifacts and delivery metadata stay tied to server truth.
---

<objective>
Populate the drawer with mission ownership, handoff, and artifact details.

Purpose: Turn the drawer shell into an operator-grade detail surface that answers what is happening now, what just happened, and what comes next.
Output: Room and agent drawer content sections backed by mission and agent data, including handoff history and linked artifacts when available.
</objective>

<execution_context>
@/Users/clawlyx/.codex/get-shit-done/workflows/execute-plan.md
@/Users/clawlyx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-detail-drawers/02-CONTEXT.md
@.planning/phases/02-detail-drawers/02-01-PLAN.md
@components/agents-virtual-office-panel.tsx
@lib/mission-control.ts
@lib/agents.ts
@app/globals.css
@locales/en.json
@locales/zh.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Join focused tasks, features, and activity context</name>
  <files>components/agents-virtual-office-panel.tsx</files>
  <action>Build the selectors needed to resolve the active task, the most recent completed task in the same feature chain, the next planned step, and the current blocker or waiting reason for the selected room or agent. Reuse the explicit ownership contract and preserve labeled inferred fallback when the live data does not identify an owner agent.</action>
  <verify>pnpm typecheck</verify>
  <done>The component can derive a coherent detail model for both room and agent selections from existing mission and agent snapshots.</done>
</task>

<task type="auto">
  <name>Task 2: Render drawer sections for ownership, handoff, and artifacts</name>
  <files>components/agents-virtual-office-panel.tsx, app/globals.css</files>
  <action>Populate the drawer with focused ownership badges, current task details, last completed task, next planned step, blocker or waiting reason, recent handoff activity, and artifact links. Surface branch and PR metadata when feature delivery data provides them, and degrade to clear placeholders when links are missing.</action>
  <verify>pnpm build</verify>
  <done>The drawer answers what is active now, what happened last, what happens next, and where the supporting artifacts live.</done>
</task>

<task type="auto">
  <name>Task 3: Localize and verify missing-data behavior</name>
  <files>locales/en.json, locales/zh.json, components/agents-virtual-office-panel.tsx</files>
  <action>Add the drawer-specific copy for ownership labels, handoff sections, artifact sections, and missing-data placeholders in both locales. Verify that empty or partially populated live data still produces a readable drawer instead of dropping entire sections without explanation.</action>
  <verify>pnpm lint</verify>
  <done>The drawer copy is localized and fallback states stay informative across room and agent selections.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `pnpm build`
- [ ] Room and agent drawers show current task, last completed task, next step, and blocker or waiting details
- [ ] Handoff and artifact sections remain readable when linked data is partially missing
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The office drawer answers current ownership and recent handoff questions without requiring a panel switch
</success_criteria>

<output>
After completion, create `.planning/phases/02-detail-drawers/02-02-SUMMARY.md`
</output>
