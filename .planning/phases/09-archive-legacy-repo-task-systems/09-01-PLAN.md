---
phase: 09-archive-legacy-repo-task-systems
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mission-control.ts
  - lib/mission-control-mutations.ts
  - app/api/mission-control/intake/route.ts
  - app/api/mission-control/tasks/[taskId]/route.ts
autonomous: true
requirements:
  - ARCH-01
  - ARCH-03
user_setup: []
must_haves:
  truths:
    - Repo-task execution stops treating `task-center`, `agent-launchpad`, and `agent-workflow` as live systems of record inside the dashboard runtime and mutation paths.
    - Mission Control reads and writes follow an explicit post-archive contract that Phase 10 can build on without inheriting the older three-repo split.
    - The app still degrades cleanly when no live runtime/task source is available, but its fallback language and data model no longer imply Launchpad-era ownership.
  artifacts:
    - lib/mission-control.ts
    - lib/mission-control-mutations.ts
    - app/api/mission-control/intake/route.ts
    - app/api/mission-control/tasks/[taskId]/route.ts
  key_links:
    - Phase 9 is the cleanup boundary for legacy repo-task plumbing, so Phase 10 must inherit a single cleaned architecture rather than adapters for retired systems.
---

<objective>
Retire the legacy repo-task runtime and mutation plumbing.

Purpose: remove the dashboard's remaining live dependence on `task-center`, `agent-launchpad`, and `agent-workflow` for repo-task execution so the runtime contract matches the post-archive architecture.
Output: a cleaned Mission Control snapshot/mutation layer that stops routing repo-task ownership through the older three-system split and exposes an explicit archive-era source model.
</objective>

<execution_context>
@/Users/clawlyx/.codex/get-shit-done/workflows/execute-plan.md
@/Users/clawlyx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-archive-legacy-repo-task-systems/09-CONTEXT.md
@lib/mission-control.ts
@lib/mission-control-mutations.ts
@app/api/mission-control/intake/route.ts
@app/api/mission-control/tasks/[taskId]/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the post-archive mission-control source contract</name>
  <files>lib/mission-control.ts</files>
  <action>Replace the legacy repo-task assumptions in the Mission Control snapshot layer with an explicit post-archive contract. Remove or isolate Launchpad-era repo/task source labels, status notes, and sample state semantics that still imply `agent-launchpad`, `agent-workflow`, or `task-center` remain the live owner for repo execution.</action>
  <verify>pnpm typecheck</verify>
  <done>The snapshot layer describes the cleaned archive-era task architecture clearly and no longer models repo-task truth around the retired systems.</done>
</task>

<task type="auto">
  <name>Task 2: Retire legacy repo-task write paths and fallback mutations</name>
  <files>lib/mission-control-mutations.ts, app/api/mission-control/intake/route.ts, app/api/mission-control/tasks/[taskId]/route.ts</files>
  <action>Remove or redirect repo-task intake and mutation flows that still depend on local Launchpad state files or Launchpad workflow APIs. Keep only the archive-compatible behavior the dashboard should still support after Phase 9, and make unsupported legacy repo-task actions fail with clear post-archive errors instead of silently preserving the old ownership model.</action>
  <verify>pnpm lint</verify>
  <done>The dashboard no longer offers repo-task mutations that rely on the retired systems, and the surviving API behavior matches the intended post-archive architecture.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve a stable downstream contract for follow-on phases</name>
  <files>lib/mission-control.ts, app/api/mission-control/tasks/[taskId]/route.ts</files>
  <action>Ensure the cleaned snapshot and task-route outputs still expose the status, provenance, and fallback fields Phase 10 will need, but sourced from the new archive-era contract rather than the earlier three-repo split.</action>
  <verify>pnpm build</verify>
  <done>Phase 10 can consume Mission Control data without inheriting retired repo-task semantics or broken shape assumptions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `pnpm build`
- [ ] API-verified mission intake and task-update routes no longer require `agent-launchpad` workflow/state ownership for repo-task execution
- [ ] Browser-verified Mission Control source/fallback messaging no longer describes the retired three-system split as the live architecture
- [ ] Code inspection confirms the cleaned snapshot contract still exposes the fields Phase 10 needs for downstream provenance and coordination work
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Repo-task execution paths in this repo no longer rely on the retired `task-center` / `agent-launchpad` / `agent-workflow` ownership model
</success_criteria>

<output>
After completion, create `.planning/phases/09-archive-legacy-repo-task-systems/09-01-SUMMARY.md`
</output>
