---
phase: 09-archive-legacy-repo-task-systems
plan: 02
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - lib/mission-control.ts
  - lib/openclaw.ts
  - demo/openclaw-home/agents/dashboard.json
autonomous: true
requirements:
  - ARCH-02
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03
user_setup: []
must_haves:
  truths:
    - Repo-bound task records are archived, migrated, or removed so they no longer pollute the live task dataset presented by the dashboard.
    - The only surviving `task-center`-style task records are legitimate personal research `TQ-XXX` items that the operator still needs.
    - The cleanup outcome is inspectable enough that the operator can tell which task records were kept versus retired instead of guessing from stale demo or fallback data.
  artifacts:
    - lib/mission-control.ts
    - lib/openclaw.ts
    - demo/openclaw-home/agents/dashboard.json
  key_links:
    - Phase 9 is not just code-path retirement; it must also remove the stale repo-bound task data that would otherwise keep the old model alive in demos and runtime hints.
---

<objective>
Clean repo-bound task data and preserve the valid personal research `TQ-XXX` set.

Purpose: archive or remove legacy repo-task records so the surviving dashboard data is intentionally personal-research-only where `task-center` lineage still exists.
Output: a task dataset and source-classification layer that explicitly keeps valid research `TQ-XXX` items while removing repo-task clutter from demo and runtime-facing data.
</objective>

<execution_context>
@/Users/clawlyx/.codex/get-shit-done/workflows/execute-plan.md
@/Users/clawlyx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-archive-legacy-repo-task-systems/09-CONTEXT.md
@.planning/phases/09-archive-legacy-repo-task-systems/09-01-PLAN.md
@lib/mission-control.ts
@lib/openclaw.ts
@demo/openclaw-home/agents/dashboard.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply explicit keep/archive/remove rules to bundled task data</name>
  <files>lib/mission-control.ts, demo/openclaw-home/agents/dashboard.json</files>
  <action>Audit every bundled repo-task and `TQ-XXX` record used by Mission Control and the demo office snapshot. Remove or archive repo-bound task rows that exist only to represent the retired systems, and preserve only the legitimate personal research `TQ-XXX` items that should still survive after the cleanup.</action>
  <verify>pnpm typecheck</verify>
  <done>The bundled runtime/demo data no longer presents repo-task clutter, and the surviving `TQ-XXX` set is intentionally personal-research-only.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten source classification around surviving personal research tasks</name>
  <files>lib/openclaw.ts</files>
  <action>Update the runtime source-classification helpers so `task-center`-like labels only survive for the valid personal research flow. Remove repo-task inference paths that would continue to describe archived repo work as if it were an active task-center stream.</action>
  <verify>pnpm lint</verify>
  <done>Runtime task-source labeling keeps personal research visibility where intended but no longer revives retired repo-task semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Make the cleanup outcome inspectable in the delivered data</name>
  <files>lib/mission-control.ts, demo/openclaw-home/agents/dashboard.json</files>
  <action>Add the minimum snapshot/demo metadata needed to show which task records were kept, archived, or removed so the post-cleanup result is explicit and later phases do not have to reverse-engineer the archive decision.</action>
  <verify>pnpm build</verify>
  <done>The delivered data makes the keep/archive/remove outcome obvious enough for operators and later phases to trust the cleanup boundary.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `pnpm build`
- [ ] Data inspection confirms repo-bound task rows were archived, migrated, or removed from bundled/live-facing datasets
- [ ] Data inspection confirms the surviving `TQ-XXX` items are intentional personal research tasks rather than repo-bound leftovers
- [ ] Browser-verified task/source hints do not surface stale repo-task clutter after the cleanup fixtures are loaded
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The surviving task dataset is explicit about what was kept versus retired, and only valid personal research `TQ-XXX` tasks remain in the task-center-like layer
</success_criteria>

<output>
After completion, create `.planning/phases/09-archive-legacy-repo-task-systems/09-02-SUMMARY.md`
</output>
