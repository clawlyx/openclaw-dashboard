---
phase: 08-agent-clarity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mission-control.ts
  - lib/openclaw.ts
  - components/agents-virtual-office-panel.tsx
  - app/globals.css
  - locales/en.json
  - locales/zh.json
autonomous: true
requirements:
  - AGENT-01
  - AGENT-02
  - AGENT-03
  - TRIAGE-01
  - TRIAGE-02
  - TRIAGE-03
  - IDLE-01
  - IDLE-02
  - IDLE-03
  - QUEUE-01
  - QUEUE-02
  - QUEUE-03
  - BLOCK-01
  - BLOCK-02
  - BLOCK-03
  - RESP-01
  - RESP-02
  - RESP-03
user_setup: []
must_haves:
  truths:
    - The Agents view becomes roster-first, so operators can scan who is working, blocked, or idle within a few seconds without losing name, task, duration, or room context.
    - Blocked agents get stronger inline reason and time context that makes intervention needs obvious without relocating task ownership truth out of Mission Control.
    - Idle suggestions stay lightweight and credible by following the required ranking rule, surfacing a compact suggestive queue, and showing an explicit no-obvious-assignment state when needed.
    - Desktop and smaller-screen layouts preserve the same Working / Blocked / Idle mental model instead of collapsing into an unstructured card wall.
  artifacts:
    - lib/mission-control.ts
    - lib/openclaw.ts
    - components/agents-virtual-office-panel.tsx
    - app/globals.css
    - locales/en.json
    - locales/zh.json
  key_links:
    - Phase 8 must make Agents easier to scan without turning it into a second Mission Control or implying automatic assignment.
---

<objective>
Rebuild the Agents view around roster clarity, blocked emphasis, and idle suggestion flow.

Purpose: make the operator's first scan immediately answer who is working, who is blocked, who is idle, and what idle agents could reasonably pick up next.
Output: a sectioned roster model, clearer blocked and idle presentation, and a responsive layout that preserves the same triage mental model across breakpoints.
</objective>

<execution_context>
@/Users/clawlyx/.codex/get-shit-done/workflows/execute-plan.md
@/Users/clawlyx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-agent-clarity/08-CONTEXT.md
@components/agents-virtual-office-panel.tsx
@lib/openclaw.ts
@lib/mission-control.ts
@app/globals.css
@locales/en.json
@locales/zh.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive a roster-first agent triage model and suggestion inputs</name>
  <files>lib/mission-control.ts, lib/openclaw.ts, components/agents-virtual-office-panel.tsx</files>
  <action>Normalize the data needed for the Agents surface so each agent can be rendered with name, status, current task, duration, and room/team context in explicit Working, Blocked, and Idle groupings. Reuse Mission Control ownership truth to compute lightweight idle suggestion candidates using the required ranking rule of same room first, then role-fit, then any ready unowned task, and expose an explicit no-obvious-assignment state when the system lacks a credible hint.</action>
  <verify>pnpm typecheck</verify>
  <done>The shared snapshot and panel logic can drive roster grouping, blocked context, and idle suggestion states without inventing a second ownership model.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild the Agents UI around Working, Blocked, and Idle sections</name>
  <files>components/agents-virtual-office-panel.tsx, app/globals.css</files>
  <action>Restructure the Agents view into clearly separated Working, Blocked, and Idle sections that prioritize roster legibility over secondary intelligence. Give blocked agents stronger visual emphasis with inline reason and duration context, add lightweight suggestion hints for idle agents, and surface a compact idle-assignment queue that stays suggestive rather than duplicating Mission Control task management.</action>
  <verify>pnpm lint</verify>
  <done>The default Agents view is visibly roster-first, intervention needs are obvious in the blocked section, and idle opportunities are easy to scan without reading as automatic assignment.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve the triage mental model across responsive layouts and localized copy</name>
  <files>components/agents-virtual-office-panel.tsx, app/globals.css, locales/en.json, locales/zh.json</files>
  <action>Adjust the desktop and small-screen layouts so the section order and scan path remain Working, Blocked, Idle, then suggestion context, even when the presentation becomes more stacked or card-like. Add or revise localized copy for blocked reasons, idle suggestion states, queue framing, and no-obvious-assignment messaging in both supported locales.</action>
  <verify>pnpm build</verify>
  <done>The Agents view keeps the same mental model on desktop and smaller screens, and all new operator-facing copy is localized in English and Chinese.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `pnpm build`
- [ ] Browser-verified Agents default view lets the operator identify Working, Blocked, and Idle sections within a few seconds on desktop
- [ ] Browser-verified blocked agents show reason and time context strongly enough that likely intervention needs are obvious without opening Mission Control first
- [ ] Browser-verified idle agents show ranked lightweight hints or an explicit no-obvious-assignment state, plus a compact suggestive queue that does not read as auto-assignment
- [ ] Browser-verified small-screen layout preserves the same Working / Blocked / Idle mental model and keeps the queue secondary to roster clarity
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The operator can open Agents and understand staffing state plus likely next work faster than before without confusion about ownership truth
</success_criteria>

<output>
After completion, create `.planning/phases/08-agent-clarity/08-01-SUMMARY.md`
</output>
