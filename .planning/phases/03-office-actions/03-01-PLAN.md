---
phase: 03-office-actions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mission-control-actions.ts
  - components/mission-task-actions.tsx
  - components/agents-virtual-office-panel.tsx
  - app/page.tsx
  - locales/en.json
  - locales/zh.json
autonomous: true
requirements:
  - ACT-01
  - ACT-02
user_setup: []
must_haves:
  truths:
    - The office surface reuses the same local and remote task-action rules as Mission Control instead of inventing a second transition model.
    - Supported task actions are clickable from the focused office drawer for the active task.
    - Unsupported actions remain visible but disabled so operators can see the full transition set and current limitations.
  artifacts:
    - lib/mission-control-actions.ts
    - components/mission-task-actions.tsx
    - components/agents-virtual-office-panel.tsx
    - app/page.tsx
    - locales/en.json
    - locales/zh.json
  key_links:
    - The office drawer action strip must share its action catalog and fetch path with `components/mission-task-actions.tsx` and `/api/mission-control/tasks/[taskId]`.
---

<objective>
Wire supported Mission Control actions into the office surface.

Purpose: Let the office drawer trigger the same task transitions as Mission Control while making disabled transitions visible.
Output: Shared action-state definitions, reusable action-strip rendering, and office drawer integration that respects local versus remote mutation rules.
</objective>

<execution_context>
@/Users/clawlyx/.codex/get-shit-done/workflows/execute-plan.md
@/Users/clawlyx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-office-actions/03-CONTEXT.md
@.planning/phases/02-detail-drawers/02-02-SUMMARY.md
@components/agents-virtual-office-panel.tsx
@components/mission-task-actions.tsx
@lib/mission-control-actions.ts
@app/api/mission-control/tasks/[taskId]/route.ts
@app/page.tsx
@locales/en.json
@locales/zh.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the shared action catalog for enabled and disabled states</name>
  <files>lib/mission-control-actions.ts</files>
  <action>Refactor the task-action helper so it can describe the full visible action set for a task in local and remote modes, including which actions are enabled versus disabled. Preserve the current server-side allow-list logic while making the UI layer aware of unavailable transitions.</action>
  <verify>pnpm typecheck</verify>
  <done>The office and Mission Control can ask for the same action catalog and know which actions are actionable versus disabled.</done>
</task>

<task type="auto">
  <name>Task 2: Reuse MissionTaskActions as a shared action strip</name>
  <files>components/mission-task-actions.tsx, app/page.tsx</files>
  <action>Adapt the existing task-action component so it can render disabled actions, preserve pending and error states, and be reused in the office surface without diverging from Mission Control copy. Thread the needed action-label copy into the office view through shared page props rather than duplicating wording.</action>
  <verify>pnpm lint</verify>
  <done>The action-strip component works for both Mission Control and the office surface, with visible disabled states and shared wording.</done>
</task>

<task type="auto">
  <name>Task 3: Add office-side action affordances for the focused task</name>
  <files>components/agents-virtual-office-panel.tsx, locales/en.json, locales/zh.json</files>
  <action>Render the shared action strip in the owner-detail drawer for the active task, and add any lightweight queue-level affordance only if it reuses the same action context. Keep the drawer as the primary action surface and make unsupported transitions visually distinct rather than hidden.</action>
  <verify>pnpm build</verify>
  <done>The office drawer exposes supported actions for the focused task and shows disabled transitions clearly when they are unavailable.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `pnpm build`
- [ ] The office drawer shows supported and disabled task actions for a live focused task
- [ ] Mission Control action rendering still works after the shared action-catalog refactor
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The office surface exposes action controls without forking Mission Control semantics
</success_criteria>

<output>
After completion, create `.planning/phases/03-office-actions/03-01-SUMMARY.md`
</output>
