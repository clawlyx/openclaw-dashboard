---
phase: 03-office-actions
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - components/agents-virtual-office-panel.tsx
  - components/mission-task-actions.tsx
  - app/api/mission-control/tasks/[taskId]/route.ts
  - lib/mission-control-actions.ts
autonomous: true
requirements:
  - ACT-01
  - ACT-02
  - ACT-03
user_setup: []
must_haves:
  truths:
    - After an office-surface mutation, the refreshed office state matches Mission Control truth in both local and remote modes.
    - The office preserves or safely repairs focus after a mutation changes the selected task or hands work to another lane.
    - Verification covers both supported action execution and disabled or unavailable transitions so the operator can trust the surface.
  artifacts:
    - components/agents-virtual-office-panel.tsx
    - components/mission-task-actions.tsx
    - app/api/mission-control/tasks/[taskId]/route.ts
    - lib/mission-control-actions.ts
  key_links:
    - Office actions still round-trip through `/api/mission-control/tasks/[taskId]` and then refresh from the snapshot path used by both dashboard surfaces.
---

<objective>
Refresh the office from source truth after task mutations and verify the supported action flows.

Purpose: Ensure the office action surface stays trustworthy after mutations instead of drifting from Mission Control.
Output: Stable post-mutation focus behavior, aligned local and remote refresh flows, and browser-verified action behavior for live and disabled cases.
</objective>

<execution_context>
@/Users/clawlyx/.codex/get-shit-done/workflows/execute-plan.md
@/Users/clawlyx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-office-actions/03-CONTEXT.md
@.planning/phases/03-office-actions/03-01-PLAN.md
@components/agents-virtual-office-panel.tsx
@components/mission-task-actions.tsx
@lib/mission-control-actions.ts
@app/api/mission-control/tasks/[taskId]/route.ts
@lib/mission-control-mutations.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preserve focused office context across task mutations</name>
  <files>components/agents-virtual-office-panel.tsx, components/mission-task-actions.tsx</files>
  <action>Make sure the office drawer stays coherent after an action completes. Preserve focus when the selected task still exists, and gracefully fall back to the owning room or refreshed owner context when the mutation advances or replaces the task.</action>
  <verify>pnpm typecheck</verify>
  <done>The office does not strand the user on stale task detail after a mutation refresh.</done>
</task>

<task type="auto">
  <name>Task 2: Align local and remote mutation feedback with source truth</name>
  <files>components/mission-task-actions.tsx, app/api/mission-control/tasks/[taskId]/route.ts, lib/mission-control-actions.ts</files>
  <action>Close any gaps between the office action surface and the server route so successful mutations, blocked transitions, and remote-mode restrictions produce the same operator feedback that Mission Control expects. Touch the route or shared action helper only where needed to keep the office refresh path trustworthy.</action>
  <verify>pnpm lint</verify>
  <done>The office and Mission Control agree on which transitions succeeded, failed, or remained unavailable in both local and remote modes.</done>
</task>

<task type="auto">
  <name>Task 3: Verify local and remote action flows end to end</name>
  <files>components/agents-virtual-office-panel.tsx</files>
  <action>Run browser verification for at least one supported office mutation in local/demo mode, one supported remote-mode mutation against a temporary Launchpad API, and one disabled/unavailable action case. Capture the final behavior in the summary so the next phase can build on a trusted action surface.</action>
  <verify>pnpm build</verify>
  <done>Office actions are browser-verified against real refresh behavior, not just static rendering.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `pnpm build`
- [ ] Browser-verified local office mutation updates the drawer and queue from refreshed truth
- [ ] Browser-verified remote office mutation updates from refreshed truth against a temporary Launchpad API
- [ ] Disabled or unsupported transitions stay visible and non-clickable
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The office action surface stays aligned with Mission Control after real mutations
</success_criteria>

<output>
After completion, create `.planning/phases/03-office-actions/03-02-SUMMARY.md`
</output>
