---
phase: 13-default-coordination-guidance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/agents.ts
  - lib/openclaw.ts
  - demo/openclaw-home/agents/dashboard.json
autonomous: true
requirements:
  - NEXT-01
  - NEXT-02
  - NEXT-03
user_setup: []
must_haves:
  truths:
    - The snapshot exposes at most one recommended coordination move for the current highest-priority issue, derived from existing overlap, handoff, and mapping evidence with severity-first ranking and no predictive staffing behavior.
    - The recommendation explicitly tells the UI whether the operator should stay in Agents or move to Mission Control, carries the best available destination context when confidence is high, and stays calm by returning no recommendation when the current state is routine.
    - Recommendation reasoning stays to one short evidence-backed sentence that explains why this move won without implying that task ownership truth moved out of Mission Control.
  artifacts:
    - lib/agents.ts
    - lib/openclaw.ts
    - demo/openclaw-home/agents/dashboard.json
  key_links:
    - Phase 12 already built overlap groups, handoff summaries, and Mission Control focus routing, so Phase 13 should rank and package one trustworthy next move from that contract instead of introducing a second coordination system or a new workspace.
---

<objective>
Build the server-side recommendation contract for Phase 13.

Purpose: turn the existing coordination model into one default operator recommendation that can be trusted at a glance.
Output: additive snapshot metadata that ranks the highest-priority coordination issue, names the destination surface, and gives the UI one concise recommendation reason plus deterministic demo coverage.
</objective>

<execution_context>
@/Users/clawlyx/.codex/get-shit-done/workflows/execute-plan.md
@/Users/clawlyx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-default-coordination-guidance/13-CONTEXT.md
@.planning/phases/12-overlap-and-handoff-visibility/12-VERIFICATION.md
@lib/agents.ts
@lib/openclaw.ts
@demo/openclaw-home/agents/dashboard.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a snapshot type for one recommended coordination move</name>
  <files>lib/agents.ts</files>
  <action>Extend the Agents snapshot contract with an optional recommendation object that can describe the chosen agent or coordination group, the destination surface (`agents` or `mission-control`), the best-known destination target, the action label, and one short trust-building reason. Keep the schema additive and backward-compatible so older live snapshots without recommendation data still render cleanly.</action>
  <verify>pnpm typecheck</verify>
  <done>The UI has one explicit, typed place to read the default recommended next move without reverse-engineering ranking rules in the browser.</done>
</task>

<task type="auto">
  <name>Task 2: Rank overlap, handoff, and mapping evidence into one winning recommendation</name>
  <files>lib/openclaw.ts</files>
  <action>Build a deterministic selector that prefers `intervene` issues first, falls back to the strongest `watch` case when no intervention exists, and returns no recommendation when the remaining coordination state is routine. The selector should reuse existing overlap groups, handoff summaries, and Mission Control destination data; stay explicit when only partial destination confidence exists; and keep reasoning to one short sentence tied to the actual evidence that made the item win.</action>
  <verify>pnpm typecheck</verify>
  <done>The snapshot produces exactly one trustworthy recommendation for the highest-priority current coordination issue, or no recommendation when the workspace is calm.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh bundled demo data so recommendation states are deterministic to verify</name>
  <files>demo/openclaw-home/agents/dashboard.json, lib/openclaw.ts</files>
  <action>Adjust the bundled demo inputs or selectors as needed so demo mode reliably produces at least one Mission Control-bound intervention recommendation, one Agents-side watch recommendation when no intervene case exists, and one calm state where no recommendation is shown. Preserve public-safe demo data and keep the phase anchored to current overlap and handoff semantics rather than invented future states.</action>
  <verify>pnpm build</verify>
  <done>Bundled demo mode gives execution and browser verification a stable recommendation story for both urgent and calm cases.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `pnpm build`
- [ ] `/api/snapshot` or demo snapshot inspection confirms the Agents payload includes either one recommendation or an explicit no-recommendation state
- [ ] Snapshot inspection confirms intervention-ranked overlap or stalled handoff issues outrank watch-only states, and watch-only states can still produce a recommendation when no intervention exists
- [ ] Snapshot inspection confirms recommendation destinations identify `Agents` vs `Mission Control` before navigation and include exact target context only when the evidence supports it
- [ ] Code review confirms recommendation reasoning stays concise, evidence-backed, advisory-only, and does not move ownership truth out of Mission Control
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The data layer exposes one trustworthy default coordination recommendation that the UI can present without recreating ranking logic client-side
</success_criteria>

<output>
After completion, create `.planning/phases/13-default-coordination-guidance/13-01-SUMMARY.md`
</output>
