---
phase: 13-default-coordination-guidance
plan: 02
type: execute
wave: 2
depends_on:
  - "13-01"
files_modified:
  - components/agents-virtual-office-panel.tsx
  - components/agents-office-panel.tsx
  - app/globals.css
  - locales/en.json
  - locales/zh.json
autonomous: true
requirements:
  - NEXT-01
  - NEXT-02
  - NEXT-03
user_setup: []
must_haves:
  truths:
    - The default Agents scan path highlights one existing coordination card or agent context as the recommended move instead of adding a new hero, banner workspace, or second coordination system.
    - The recommendation tells the operator up front whether to stay in Agents or jump to Mission Control, names the exact destination when confidence is high, and keeps the reason to one short sentence tied to visible overlap, handoff, or mapping evidence.
    - Routine states remain calm: when no meaningful next move exists, the coordination surface does not manufacture urgency or fake certainty.
  artifacts:
    - components/agents-virtual-office-panel.tsx
    - components/agents-office-panel.tsx
    - app/globals.css
    - locales/en.json
    - locales/zh.json
  key_links:
    - Phase 10 made the default Agents view concise and Phase 12 already renders overlap and handoff snippets, so Phase 13 should elevate one of those existing items into a recommended next move rather than introducing a new top-level coordination shell.
---

<objective>
Expose the recommended next move directly in the existing Agents coordination surface.

Purpose: make the default Agents scan path answer “what should I do next, and where should I do it?” without forcing the operator into a second coordination view.
Output: highlighted recommendation treatment, explicit destination labeling, and concise advisory copy on the existing Agents panels.
</objective>

<execution_context>
@/Users/clawlyx/.codex/get-shit-done/workflows/execute-plan.md
@/Users/clawlyx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-default-coordination-guidance/13-CONTEXT.md
@.planning/phases/13-default-coordination-guidance/13-01-PLAN.md
@components/agents-virtual-office-panel.tsx
@components/agents-office-panel.tsx
@app/globals.css
@locales/en.json
@locales/zh.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Highlight one existing coordination item as the default recommended move</name>
  <files>components/agents-virtual-office-panel.tsx, components/agents-office-panel.tsx, app/globals.css</files>
  <action>Update the default Agents coordination panels so one existing workload or coordination snippet can render as the recommended next move with stronger emphasis, while the rest of the surface stays concise. Reuse existing cards, snippets, and drawers instead of inventing a new banner or route. If the recommendation is Agents-local, the affordance should focus the relevant agent, room, or detail state directly.</action>
  <verify>pnpm lint</verify>
  <done>The operator can see one recommended move directly inside the normal Agents scan path and follow it without searching for the right card first.</done>
</task>

<task type="auto">
  <name>Task 2: Show destination clarity and concise trust-building reasoning before navigation</name>
  <files>components/agents-virtual-office-panel.tsx, components/agents-office-panel.tsx, locales/en.json, locales/zh.json</files>
  <action>Add copy and metadata that tell the operator whether the move belongs in Agents or Mission Control, name the best-known destination target when confidence is high, and explain the winning reason in one short sentence tied to current mapping, overlap, or handoff evidence. When confidence is partial, stay explicit about the destination surface without pretending there is an exact hidden match.</action>
  <verify>pnpm typecheck</verify>
  <done>The recommendation reads as trustworthy guidance rather than a vague urgency badge, and the operator knows where they will land before they click.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve a calm default when no recommendation is warranted</name>
  <files>components/agents-virtual-office-panel.tsx, components/agents-office-panel.tsx, app/globals.css, locales/en.json, locales/zh.json</files>
  <action>Refine empty and routine states so the coordination surface remains calm when the snapshot does not produce a meaningful next move. Healthy parallel work and routine handoffs should still be visible, but the recommendation slot should clearly communicate that no action needs escalation right now instead of manufacturing urgency.</action>
  <verify>pnpm build</verify>
  <done>The default Agents scan path stays trustworthy in both urgent and healthy states because it only recommends action when the evidence supports one.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `pnpm build`
- [ ] Browser verification on `/?view=agents&panel=virtual` confirms one existing coordination card is highlighted as the recommendation when the snapshot exposes a winning issue
- [ ] Browser verification confirms the recommended move labels the destination surface as `Agents` or `Mission Control` before navigation and shows the best-known target context when available
- [ ] Browser verification confirms the recommendation reason stays to one short sentence tied to visible coordination evidence rather than generic urgency wording
- [ ] Browser verification confirms calm demo states do not force a recommendation and that the coordination surface still reads clearly on desktop and smaller screens
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The default Agents surface presents one trustworthy next move with explicit destination clarity and no new coordination workspace
</success_criteria>

<output>
After completion, create `.planning/phases/13-default-coordination-guidance/13-02-SUMMARY.md`
</output>
