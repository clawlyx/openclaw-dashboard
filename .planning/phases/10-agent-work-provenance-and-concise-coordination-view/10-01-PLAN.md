---
phase: 10-agent-work-provenance-and-concise-coordination-view
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/agents.ts
  - lib/openclaw.ts
  - demo/openclaw-home/agents/dashboard.json
autonomous: true
requirements:
  - PROV-01
  - PROV-02
  - PROV-03
  - SUG-01
  - SUG-02
user_setup: []
must_haves:
  truths:
    - The server snapshot exposes explicit repo or intake-thread provenance for working agents instead of forcing the client to guess from room labels.
    - Agents with multiple active sessions can expose more than one live workload entry so the operator can see concurrent work instead of a collapsed single-task state.
    - Idle suggestion inputs expand beyond the current Mission Control-only queue to include tracked repo work and actionable personal research items without reviving archived repo-task ownership semantics.
  artifacts:
    - lib/agents.ts
    - lib/openclaw.ts
    - demo/openclaw-home/agents/dashboard.json
  key_links:
    - Phase 9 archived the legacy repo-task bridge, so new provenance and suggestion sources must remain advisory and metadata-driven rather than restoring old runtime ownership paths.
---

<objective>
Build the live provenance and advisory-source contract for Phase 10.

Purpose: give the Agents surfaces a trustworthy server-side model for repo-work, intake-thread research, concurrent sessions, and broader suggestion inputs.
Output: additive snapshot fields and demo fixtures that describe what each working agent is serving and what credible next work exists for idle agents.
</objective>

<execution_context>
@/Users/clawlyx/.codex/get-shit-done/workflows/execute-plan.md
@/Users/clawlyx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-agent-work-provenance-and-concise-coordination-view/10-CONTEXT.md
@.planning/phases/10-agent-work-provenance-and-concise-coordination-view/10-RESEARCH.md
@lib/agents.ts
@lib/openclaw.ts
@demo/openclaw-home/agents/dashboard.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the agent snapshot schema for explicit workload provenance</name>
  <files>lib/agents.ts</files>
  <action>Add optional snapshot fields that describe working provenance explicitly: repo/thread labels, workload summaries, provenance source notes, and multiple active-session entries where the metadata supports them. Keep configured snapshots, derived session parsing, and fallback behavior backward-compatible so older local installs still render.</action>
  <verify>pnpm typecheck</verify>
  <done>The server contract can tell the UI which repo or intake thread a working agent is serving, including partial or multi-session cases, without collapsing everything into a room-only label.</done>
</task>

<task type="auto">
  <name>Task 2: Aggregate advisory suggestion inputs from tracked repo and research backlogs</name>
  <files>lib/openclaw.ts, lib/agents.ts</files>
  <action>Introduce a server-side advisory-source reader that can surface open tracked repo GSD work plus actionable personal research items and pass those candidates into the Agents snapshot. Preserve the advisory boundary explicitly: suggestions explain why they were ranked, but they do not become task ownership truth or revive archived repo-task systems.</action>
  <verify>pnpm typecheck</verify>
  <done>The snapshot includes richer idle suggestion candidates and ranking reasons sourced from current repo/research metadata rather than only ready unowned Mission Control tasks.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh demo provenance fixtures for deterministic verification</name>
  <files>demo/openclaw-home/agents/dashboard.json</files>
  <action>Update the bundled demo snapshot so it exercises repo-work provenance, intake-thread research provenance, advisory suggestion reasons, and at least one multi-session working-agent case. The fixture should make Phase 10 browser verification possible even when the local OpenClaw home lacks rich metadata.</action>
  <verify>pnpm build</verify>
  <done>The repo ships with deterministic demo data that proves the new provenance and suggestion contract in both the browser and `/api/snapshot` output.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `pnpm build`
- [ ] `/api/snapshot` inspection confirms working-agent provenance and idle suggestion fields are present in the server snapshot contract
- [ ] Demo data review confirms at least one repo-work case, one intake-research case, and one multi-session case render from bundled fixtures
- [ ] Code inspection confirms no archived `task-center` / `agent-launchpad` / `agent-workflow` ownership semantics were restored while adding advisory sources
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The Agents data contract is rich enough for downstream UI work to show trustworthy provenance and broader advisory suggestions without client-side guesswork
</success_criteria>

<output>
After completion, create `.planning/phases/10-agent-work-provenance-and-concise-coordination-view/10-01-SUMMARY.md`
</output>
