---
phase: 10-agent-work-provenance-and-concise-coordination-view
plan: 02
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - components/agents-virtual-office-panel.tsx
  - components/agents-office-panel.tsx
  - app/globals.css
  - locales/en.json
  - locales/zh.json
autonomous: true
requirements:
  - PROV-01
  - PROV-03
  - SUG-03
  - SIMP-01
  - SIMP-02
  - SIMP-03
user_setup: []
must_haves:
  truths:
    - Working agents show concrete repo or research-thread provenance inline, and multi-session agents expose enough context for the operator to trust what they are serving.
    - Idle suggestions stay clearly advisory by explaining source and ranking reason instead of reading like automatic staffing.
    - The lower half of the Agents experience becomes a concise coordination layer focused on active work, likely next moves, and intervention points rather than a wall of secondary rails.
  artifacts:
    - components/agents-virtual-office-panel.tsx
    - components/agents-office-panel.tsx
    - app/globals.css
    - locales/en.json
    - locales/zh.json
  key_links:
    - Phase 8 already established Working / Blocked / Idle triage, so Phase 10 should build on that scan path rather than replacing it with another analytics-heavy surface.
---

<objective>
Expose live provenance and concise coordination directly in the Agents UI.

Purpose: turn the richer Phase 10 snapshot contract into a trustworthy operator experience that shows what working agents are serving, why idle suggestions exist, and what coordination needs attention next.
Output: updated Agents surfaces, localized copy, and styling that make provenance and coordination easier to scan than the current lower-half layout.
</objective>

<execution_context>
@/Users/clawlyx/.codex/get-shit-done/workflows/execute-plan.md
@/Users/clawlyx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-agent-work-provenance-and-concise-coordination-view/10-CONTEXT.md
@.planning/phases/10-agent-work-provenance-and-concise-coordination-view/10-RESEARCH.md
@.planning/phases/10-agent-work-provenance-and-concise-coordination-view/10-01-PLAN.md
@components/agents-virtual-office-panel.tsx
@components/agents-office-panel.tsx
@app/globals.css
@locales/en.json
@locales/zh.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Show explicit work provenance in the working roster</name>
  <files>components/agents-virtual-office-panel.tsx, components/agents-office-panel.tsx, locales/en.json, locales/zh.json</files>
  <action>Update the roster cards and related focused views so working agents show repo or intake-thread provenance, source/fallback notes, and multi-session context when available. Preserve a readable fallback state when metadata is partial, but make it obvious that the UI is showing an inferred or partial view rather than exact truth.</action>
  <verify>pnpm lint</verify>
  <done>Operators can identify what each working agent is serving without opening the thread first, and multi-session cases no longer collapse into one ambiguous desk state.</done>
</task>

<task type="auto">
  <name>Task 2: Rework idle suggestion presentation around advisory reasoning</name>
  <files>components/agents-virtual-office-panel.tsx, components/agents-office-panel.tsx, locales/en.json, locales/zh.json</files>
  <action>Replace the current narrow suggestion language with cards and queue entries that show advisory source, ranking reason, and whether the candidate came from repo work or personal research. Keep the tone explicitly suggestive and avoid any copy that sounds like automatic assignment or hidden ownership changes.</action>
  <verify>pnpm typecheck</verify>
  <done>Idle suggestion entries communicate why a next move is being suggested and where it came from, while remaining clearly advisory.</done>
</task>

<task type="auto">
  <name>Task 3: Simplify the lower-half Agents layout into a concise coordination surface</name>
  <files>components/agents-virtual-office-panel.tsx, app/globals.css</files>
  <action>Collapse or remove the most overwhelming lower-half rails and replace them with a tighter coordination layer that highlights active work, likely handoffs, and intervention needs. Preserve focused drill-down access where useful, but shorten the default scan path so the operator sees the most helpful next-action information first.</action>
  <verify>pnpm build</verify>
  <done>The default Agents page feels materially more concise, with coordination value preserved and secondary analytics no longer dominating the lower half.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `pnpm build`
- [ ] Browser-verified working-agent cards show repo/thread provenance and multi-session context without ambiguous room-only labels
- [ ] Browser-verified idle suggestions explain source and ranking reason while staying clearly advisory
- [ ] Browser-verified lower-half layout is shorter and more coordination-focused than the pre-phase baseline on both desktop and smaller screens
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The Agents UI makes work provenance and likely coordination actions easier to scan than the current lower-half layout
</success_criteria>

<output>
After completion, create `.planning/phases/10-agent-work-provenance-and-concise-coordination-view/10-02-SUMMARY.md`
</output>
