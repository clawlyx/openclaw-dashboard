---
phase: 12-overlap-and-handoff-visibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/agents.ts
  - lib/openclaw.ts
  - lib/mission-control.ts
  - demo/openclaw-home/agents/dashboard.json
autonomous: true
requirements:
  - HAND-01
  - HAND-02
  - HAND-03
user_setup: []
must_haves:
  truths:
    - The server snapshot exposes explicit overlap and handoff metadata so the UI can distinguish healthy parallel work from risky coordination ambiguity without guessing from room labels alone.
    - Active work carries recent handoff state that names the last involved agent and the expected next owner or lane when trustworthy evidence exists, while missing evidence stays explicit instead of faking certainty.
    - Intervention priority is derived from current overlap and handoff ambiguity, not predictive forecasting, and Mission Control remains the ownership system of record.
  artifacts:
    - lib/agents.ts
    - lib/openclaw.ts
    - lib/mission-control.ts
    - demo/openclaw-home/agents/dashboard.json
  key_links:
    - Phase 11 already established explicit Mission Control mapping and handoff navigation, so Phase 12 should extend that contract with overlap and handoff clarity instead of creating a second coordination model in the client.
---

<objective>
Build the overlap and handoff data contract for Phase 12.

Purpose: give the Agents and Mission Control surfaces one trustworthy snapshot description of where multiple agents are serving the same mission or nearby work, whether that looks intentional or ambiguous, and what the latest handoff state says about next ownership.
Output: additive coordination metadata plus deterministic demo sample cases for healthy parallel work, risky overlap, and active handoffs.
</objective>

<execution_context>
@/Users/clawlyx/.codex/get-shit-done/workflows/execute-plan.md
@/Users/clawlyx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-mission-to-agent-mapping-clarity/11-VERIFICATION.md
@lib/agents.ts
@lib/openclaw.ts
@lib/mission-control.ts
@demo/openclaw-home/agents/dashboard.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the snapshot schema for overlap groups, handoff summaries, and intervention priority</name>
  <files>lib/agents.ts</files>
  <action>Add additive snapshot types that can represent shared-work groups, recent handoff summaries, ambiguity state, and intervention priority at the agent or coordination level. Keep the schema backward-compatible so existing configured snapshots and older local installs still render when the new metadata is absent.</action>
  <verify>pnpm typecheck</verify>
  <done>The server contract can describe healthy parallel work, risky overlap, and active handoff state without breaking older snapshot readers.</done>
</task>

<task type="auto">
  <name>Task 2: Derive overlap and handoff state from Mission Control, workload, and activity evidence</name>
  <files>lib/openclaw.ts, lib/mission-control.ts</files>
  <action>Build server-side coordination selectors that cluster agents serving the same task, feature, or tightly related work; classify those groups as intentional parallelism or ambiguous overlap; and derive recent handoff state from task ownership, history, lane context, and fresh agent activity. Treat missing evidence as an explicit unknown state and preserve the archive-era boundary so no retired repo-task ownership path reappears.</action>
  <verify>pnpm typecheck</verify>
  <done>The dashboard snapshot exposes one normalized overlap and handoff model that downstream UI code can trust without recreating matching logic in the browser.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh bundled sample data for deterministic overlap and handoff verification</name>
  <files>demo/openclaw-home/agents/dashboard.json, lib/mission-control.ts</files>
  <action>Update the bundled agent snapshot and Mission Control sample state so Phase 12 can be verified in demo mode with at least one healthy shared-mission group, one ambiguous overlap that needs intervention, and one active handoff with trustworthy last-agent and next-owner-or-lane context.</action>
  <verify>pnpm build</verify>
  <done>The repo ships with deterministic demo coverage for the exact overlap and handoff states the new UI must present.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `pnpm build`
- [ ] `/api/snapshot` or demo snapshot inspection confirms the server contract distinguishes healthy parallel work from overlap that needs intervention
- [ ] Demo verification confirms at least one healthy shared-work group, one ambiguous overlap group, and one active handoff with last-agent plus next-owner-or-lane context
- [ ] Code inspection confirms Mission Control remains the ownership system of record and that unknown handoff evidence stays explicit instead of reading like predictive certainty
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The Phase 12 UI can rely on one explicit coordination contract for overlap, handoff state, and intervention priority
</success_criteria>

<output>
After completion, create `.planning/phases/12-overlap-and-handoff-visibility/12-01-SUMMARY.md`
</output>
