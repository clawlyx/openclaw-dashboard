---
phase: 12-overlap-and-handoff-visibility
plan: 02
type: execute
wave: 2
depends_on:
  - "12-01"
files_modified:
  - components/agents-virtual-office-panel.tsx
  - components/agents-office-panel.tsx
  - app/globals.css
  - locales/en.json
  - locales/zh.json
autonomous: true
requirements:
  - HAND-01
  - HAND-02
  - HAND-03
user_setup: []
must_haves:
  truths:
    - The default Agents scan path shows when multiple agents are serving the same mission or nearby work and makes clear whether that grouping looks intentional or ambiguous.
    - Recent handoff state is visible in the relevant existing agent and mission detail flows, including the last involved agent and the expected next owner or lane when known, while the work stays inside current cards, drawers, and lists rather than a net-new coordination workspace.
    - Cases that need intervention stand out above routine parallel work without reintroducing the dense lower-half analytics layout Phase 10 intentionally removed or adding a new coordination rail.
  artifacts:
    - components/agents-virtual-office-panel.tsx
    - components/agents-office-panel.tsx
    - app/globals.css
    - locales/en.json
    - locales/zh.json
  key_links:
    - Phase 10 made the Agents surface concise and Phase 11 made Mission Control mapping explicit, so Phase 12 should layer overlap and handoff cues onto that existing scan path instead of adding another coordination workspace.
---

<objective>
Expose overlap and handoff clarity directly in the Agents surfaces.

Purpose: turn the new coordination metadata into a faster operator scan path that shows when shared work is safe, when it is ambiguous, and where a handoff is about to stall.
Output: updated Agents views, localized copy, and responsive styling for overlap groups, handoff status, and intervention priority.
</objective>

<execution_context>
@/Users/clawlyx/.codex/get-shit-done/workflows/execute-plan.md
@/Users/clawlyx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-overlap-and-handoff-visibility/12-01-PLAN.md
@components/agents-virtual-office-panel.tsx
@components/agents-office-panel.tsx
@app/globals.css
@locales/en.json
@locales/zh.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Show healthy parallel work and risky overlap in the default Agents scan path</name>
  <files>components/agents-virtual-office-panel.tsx, components/agents-office-panel.tsx, locales/en.json, locales/zh.json</files>
  <action>Update the working roster, concise coordination brief, and supporting Agents panels so they group related active work, label it as healthy parallelism or risky overlap, and explain the evidence behind that label without implying ownership moved out of Mission Control.</action>
  <verify>pnpm lint</verify>
  <done>Operators can tell whether multiple active agents are collaborating intentionally or duplicating effort from the default Agents scan path.</done>
</task>

<task type="auto">
  <name>Task 2: Surface recent handoff state in focused agent and mission details</name>
  <files>components/agents-virtual-office-panel.tsx, components/agents-office-panel.tsx, locales/en.json, locales/zh.json</files>
  <action>Expand the relevant detail cards, drawers, and list rows so active work shows recent handoff context including the last involved agent, the expected next owner or lane when known, and an explicit unknown state when the data does not support a trustworthy answer. Keep this within the existing Agents scan path and detail flows instead of introducing another coordination surface.</action>
  <verify>pnpm typecheck</verify>
  <done>The operator can inspect handoff state without opening a separate coordination workspace or reverse-engineering it from raw task history.</done>
</task>

<task type="auto">
  <name>Task 3: Elevate intervention-needed cases without losing the concise coordination layout</name>
  <files>components/agents-virtual-office-panel.tsx, components/agents-office-panel.tsx, app/globals.css, locales/en.json, locales/zh.json</files>
  <action>Refine badges, layout priority, emphasis, and copy so overlap or handoff ambiguity that needs intervention reads as higher urgency than routine shared work, while the overall page still feels shorter and more actionable than the older analytics-heavy lower half.</action>
  <verify>pnpm build</verify>
  <done>Intervention-worthy coordination issues become obvious before routine parallel activity, and the default Agents experience stays concise on desktop and smaller screens.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `pnpm build`
- [ ] Browser verification on `/?view=agents&panel=virtual` confirms shared active work is labeled as healthy parallelism or risky overlap with readable evidence
- [ ] Browser verification confirms focused agent or mission views show recent handoff context with last-agent and next-owner-or-lane details when available
- [ ] Desktop and narrow-width review confirm intervention-needed states are more visually prominent than routine parallel work without reintroducing dense secondary rails
- [ ] Component and route review confirms overlap and handoff visibility stays on existing Agents surfaces with no net-new coordination workspace or route
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The Agents surface makes overlap, handoff state, and intervention priority obvious from the default scan path
</success_criteria>

<output>
After completion, create `.planning/phases/12-overlap-and-handoff-visibility/12-02-SUMMARY.md`
</output>
