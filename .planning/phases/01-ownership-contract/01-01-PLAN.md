---
phase: 01-ownership-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mission-control.ts
  - lib/agents.ts
  - demo/openclaw-home/agents/dashboard.json
autonomous: true
requirements:
  - OWN-01
  - OWN-02
  - OWN-04
user_setup: []
must_haves:
  truths:
    - Mission Control task snapshots expose optional explicit ownership metadata without breaking existing readers.
    - Agent snapshots expose optional structured task references while preserving current free-text fields.
    - Demo data exercises the explicit ownership path with repo-safe sample values.
  artifacts:
    - lib/mission-control.ts
    - lib/agents.ts
    - demo/openclaw-home/agents/dashboard.json
  key_links:
    - The dashboard snapshot returned by lib/openclaw.ts carries the new ownership fields through to /api/snapshot.
---

<objective>
Extend the ownership contract in the snapshot layer.

Purpose: Establish the data model that later drawer and action phases depend on.
Output: Mission Control task ownership fields, agent task reference fields, and demo data that exercises the happy path.
</objective>

<execution_context>
@/Users/clawlyx/.codex/get-shit-done/workflows/execute-plan.md
@/Users/clawlyx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ownership-contract/01-CONTEXT.md
@lib/mission-control.ts
@lib/agents.ts
@lib/openclaw.ts
@demo/openclaw-home/agents/dashboard.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend snapshot types and normalization</name>
  <files>lib/mission-control.ts, lib/agents.ts</files>
  <action>Add optional explicit ownership fields to mission task snapshots and optional structured task reference fields to agent snapshots. Update normalization so missing fields stay undefined instead of throwing or forcing fake values. Preserve the existing `currentTask`, `focus`, and `nextHandoff` fields because later phases still rely on them.</action>
  <verify>pnpm typecheck</verify>
  <done>Type definitions and snapshot readers accept the new fields and still normalize older data safely.</done>
</task>

<task type="auto">
  <name>Task 2: Seed safe demo ownership data</name>
  <files>lib/mission-control.ts, demo/openclaw-home/agents/dashboard.json</files>
  <action>Update the bundled Mission Control sample and the demo agent dashboard so at least one mission task and one agent exercise explicit ownership and task-reference fields. Keep the values synthetic and consistent across the two demo sources.</action>
  <verify>pnpm typecheck</verify>
  <done>The bundled demo snapshot contains at least one explicit owner agent/room example and matching task references.</done>
</task>

<task type="auto">
  <name>Task 3: Verify snapshot continuity</name>
  <files>lib/openclaw.ts</files>
  <action>Confirm the composed dashboard snapshot still carries agents and missionControl data through unchanged. Touch `lib/openclaw.ts` only if typing or composition needs adjustment for the new contract.</action>
  <verify>pnpm typecheck</verify>
  <done>`/api/snapshot` can expose the extended fields without requiring new API wiring.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `/api/snapshot` exposes the new fields in demo mode
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Ownership and task-reference fields are optional, typed, and demo-backed
</success_criteria>

<output>
After completion, create `.planning/phases/01-ownership-contract/01-01-SUMMARY.md`
</output>
