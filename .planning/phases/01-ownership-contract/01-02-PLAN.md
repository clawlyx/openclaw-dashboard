---
phase: 01-ownership-contract
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - components/agents-virtual-office-panel.tsx
  - locales/en.json
  - locales/zh.json
autonomous: true
requirements:
  - OWN-03
user_setup: []
must_haves:
  truths:
    - The office surface visibly distinguishes explicit ownership from inferred fallback.
    - Existing room mission coverage remains readable when live data does not include the new fields.
  artifacts:
    - components/agents-virtual-office-panel.tsx
    - locales/en.json
    - locales/zh.json
  key_links:
    - Office mission cards render the ownership source using the extended snapshot data from plan 01-01.
---

<objective>
Expose ownership source clearly in the office UI.

Purpose: Prevent inferred fallback from being mistaken for explicit ownership.
Output: Minimal office-surface labels and copy updates that distinguish explicit ownership from inferred fallback.
</objective>

<execution_context>
@/Users/clawlyx/.codex/get-shit-done/workflows/execute-plan.md
@/Users/clawlyx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ownership-contract/01-CONTEXT.md
@.planning/phases/01-ownership-contract/01-01-PLAN.md
@components/agents-virtual-office-panel.tsx
@locales/en.json
@locales/zh.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread ownership source through the office panel</name>
  <files>components/agents-virtual-office-panel.tsx</files>
  <action>Update the room-mission and queue presentation layer so explicit ownership and inferred fallback can be rendered differently without disrupting the current layout. Keep the UI changes minimal and focused on clarity.</action>
  <verify>pnpm typecheck</verify>
  <done>The office surface can show explicit ownership when available and a labeled inferred fallback when not.</done>
</task>

<task type="auto">
  <name>Task 2: Localize the ownership-source copy</name>
  <files>locales/en.json, locales/zh.json</files>
  <action>Add the text needed to label explicit and inferred ownership in both supported locales. Preserve the current locale structure and wording style.</action>
  <verify>pnpm typecheck</verify>
  <done>Both locales include the new ownership-source strings used by the office panel.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] Office mission ownership source is distinguishable in the UI
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Inferred fallback is no longer visually ambiguous
</success_criteria>

<output>
After completion, create `.planning/phases/01-ownership-contract/01-02-SUMMARY.md`
</output>
