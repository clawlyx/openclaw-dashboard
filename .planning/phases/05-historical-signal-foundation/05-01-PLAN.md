---
phase: 05-historical-signal-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mission-control.ts
  - lib/pressure-signals.ts
autonomous: true
requirements:
  - HIST-01
  - HIST-02
user_setup: []
must_haves:
  truths:
    - Historical signal data comes from explainable mission/task timestamps and recorded history, not hidden heuristics.
    - Active task pressure remains separate from historical trend data so later UI layers can show both clearly.
    - Missing or partial timeline fields still produce a readable, fallback-safe history model.
  artifacts:
    - lib/mission-control.ts
    - lib/pressure-signals.ts
  key_links:
    - The derived history model must remain consistent with existing Mission Control queue truth and explicit ownership fields.
---

<objective>
Add a historical signal foundation to the mission data layer.

Purpose: make the workstation capable of reasoning about aging, wait duration, and recent pressure movement from recorded task and feature timelines.
Output: normalized history-aware mission/task snapshot shaping plus reusable derivation helpers that preserve live and demo fallback behavior.
</objective>

<execution_context>
@/Users/clawlyx/.codex/get-shit-done/workflows/execute-plan.md
@/Users/clawlyx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-pressure-signals/04-01-SUMMARY.md
@.planning/phases/04-pressure-signals/04-02-SUMMARY.md
@lib/mission-control.ts
@lib/pressure-signals.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend mission snapshots with timeline-safe historical records</name>
  <files>lib/mission-control.ts</files>
  <action>Introduce normalized feature or task history structures that preserve the timestamps needed for aging, review wait, and blocked duration analysis. Parse live Launchpad state and bundled demo state defensively so missing history fields do not break the snapshot contract.</action>
  <verify>pnpm typecheck</verify>
  <done>Mission Control exposes a history-aware snapshot shape that future intelligence views can trust.</done>
</task>

<task type="auto">
  <name>Task 2: Build reusable historical signal derivation helpers</name>
  <files>lib/pressure-signals.ts</files>
  <action>Add helpers that turn snapshot timelines into explainable historical metrics such as task age, review wait duration, blocked duration, and recent trend deltas. Keep current-pressure scoring separate from the new history outputs so Phase 6 can compose them without ambiguity.</action>
  <verify>pnpm lint</verify>
  <done>The codebase has reusable derivation helpers for historical pressure and wait-time analysis.</done>
</task>

<task type="auto">
  <name>Task 3: Prove fallback-safe behavior for partial timelines</name>
  <files>lib/mission-control.ts, lib/pressure-signals.ts</files>
  <action>Cover the main fallback paths for tasks that only have updated timestamps, only have partial history arrays, or only have ownership fields without recent activity. Ensure the resulting derived metrics stay readable rather than collapsing to invalid or misleading values.</action>
  <verify>pnpm build</verify>
  <done>Historical signal derivation remains stable in both live and demo fallback modes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `pnpm build`
- [ ] `/api/snapshot` exposes history-aware mission/task data without breaking existing queue fields
- [ ] Historical helpers distinguish current pressure from derived timeline metrics
- [ ] Partial timeline data still produces readable aging or wait values
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The codebase has a clean, reusable historical-signal foundation for later operator-intelligence surfaces
</success_criteria>

<output>
After completion, create `.planning/phases/05-historical-signal-foundation/05-01-SUMMARY.md`
</output>
