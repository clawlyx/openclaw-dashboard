---
phase: 05-historical-signal-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - components/agents-virtual-office-panel.tsx
  - app/page.tsx
  - lib/agents.ts
  - lib/mission-control.ts
  - demo/openclaw-home/agents/dashboard.json
autonomous: true
requirements:
  - HIST-01
  - HIST-02
user_setup: []
must_haves:
  truths:
    - Room and mission models can consume historical metrics without overwriting current ownership and current-pressure truth.
    - Review wait, blocked duration, and age trends stay readable even when some rooms or tasks only have partial history.
    - The demo dataset and agent task references exercise the new historical model safely.
  artifacts:
    - components/agents-virtual-office-panel.tsx
    - app/page.tsx
    - lib/agents.ts
    - lib/mission-control.ts
    - demo/openclaw-home/agents/dashboard.json
  key_links:
    - Phase 5 should add consumable trend data and lightweight hooks, while Phase 6 remains responsible for the larger intelligence surfaces.
---

<objective>
Thread historical metrics into the office data model and fallback-safe UI hooks.

Purpose: make the existing workstation surfaces aware of age and wait-time trend data so later intelligence views can build on a proven contract.
Output: room and mission data models that carry historical metrics, plus minimal office-facing hooks that prove the data is consumable without turning Phase 5 into the full intelligence UI.
</objective>

<execution_context>
@/Users/clawlyx/.codex/get-shit-done/workflows/execute-plan.md
@/Users/clawlyx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-historical-signal-foundation/05-01-PLAN.md
@components/agents-virtual-office-panel.tsx
@app/page.tsx
@lib/agents.ts
@lib/mission-control.ts
@demo/openclaw-home/agents/dashboard.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread trend metrics into room and mission view models</name>
  <files>components/agents-virtual-office-panel.tsx, app/page.tsx, lib/agents.ts, lib/mission-control.ts</files>
  <action>Wire the historical age and wait-time outputs from Phase 5 into the room, mission, and detail-drawer view models. Keep the contract explicit so the UI can tell whether a value is derived from full history, partial fallback, or current-state only data.</action>
  <verify>pnpm typecheck</verify>
  <done>The office data model carries room and mission trend metrics without collapsing into inferred-only logic.</done>
</task>

<task type="auto">
  <name>Task 2: Add lightweight fallback-safe trend cues</name>
  <files>components/agents-virtual-office-panel.tsx, app/page.tsx</files>
  <action>Add minimal trend or aging hooks to the existing office surfaces and detail drawer so operators can verify the historical model is working. Keep the UI conservative: this phase should prove the contract, not deliver the full operator-intelligence comparison surfaces planned for Phase 6.</action>
  <verify>pnpm lint</verify>
  <done>The office visibly exposes basic historical context without overreaching into the next phase.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh bundled demo data for trend coverage</name>
  <files>lib/mission-control.ts, demo/openclaw-home/agents/dashboard.json</files>
  <action>Adjust the bundled sample missions and agent task references so at least one active mission exercises aging, review wait, and blocked duration trends using only safe demo data. Ensure fallback paths remain visible for tasks with sparse histories.</action>
  <verify>pnpm build</verify>
  <done>The bundled dataset demonstrates the new historical signal foundation without private data.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `pnpm build`
- [ ] Browser-verified office surface shows at least one aging or wait-time trend cue from bundled demo data
- [ ] Browser-verified detail drawer still separates current state from historical context
- [ ] Demo fallback mode still renders readable values when a task has sparse timeline data
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 5 leaves the codebase with a trustworthy historical-metrics contract ready for Phase 6 intelligence surfaces
</success_criteria>

<output>
After completion, create `.planning/phases/05-historical-signal-foundation/05-02-SUMMARY.md`
</output>
